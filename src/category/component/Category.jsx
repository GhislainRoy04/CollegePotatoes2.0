'use strict';
import React from "react";

//TODO: Create different category that will possibly redirect? Category should have sub-category? Category should also be in the sub header

export const Category = () =>(
    <div>
        <label>Category</label>
    </div>
);

export default (Category);